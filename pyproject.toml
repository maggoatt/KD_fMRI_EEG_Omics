[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kd-fmri-eeg-omics"
version = "0.1.0"
description = "KD fMRI EEG Omics"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["preprocessing*", "model*"]
